# This is NGINX configuration for hosting static resources
server {
        listen 80;
        listen [::]:80;
        server_name localhost 127.0.0.1;

        root /var/www/html/dist;
        index index.html;

        location / {
                try_files $uri $uri/ =404;
        }
}
